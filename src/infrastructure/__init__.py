"""Infrastructure layer.

Implements interfaces defined in domain/application. All I/O lives here:
databases, caches, HTTP clients, external APIs. Reads environment config.
Contains no business logic.
"""
